{
  "name": "heat-aware-tourism-guide",
  "private": true,
  "scripts": {
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "python:format:check": ".venv/bin/ruff format --check app tests",
    "python:lint": ".venv/bin/ruff check app tests",
    "python:test": ".venv/bin/python -m pytest -q -m \"not integration\"",
    "python:test:integration": ".venv/bin/python -m pytest -q -m integration",
    "python:typecheck": ".venv/bin/python -m mypy app tests",
    "frontend:format:check": "prettier --check frontend",
    "frontend:lint": "npm --prefix frontend run lint",
    "frontend:typecheck": "npm --prefix frontend run typecheck",
    "frontend:test": "npm --prefix frontend run test",
    "backend:dev": ".venv/bin/uvicorn app.main:app --reload",
    "frontend:dev": "npm --prefix frontend run dev",
    "frontend:build": "npm --prefix frontend run build",
    "e2e": "npm --prefix frontend run test:e2e",
    "typecheck": "npm run python:typecheck && npm run frontend:typecheck",
    "test": "npm run python:test && npm run frontend:test",
    "prepare": "husky"
  },
  "devDependencies": {
    "husky": "^9.1.7",
    "lint-staged": "^17.3.0",
    "prettier": "^3.6.2"
  }
}
